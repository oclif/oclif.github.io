{
  "license": "BSD-3-Clause",
  "scripts": {
    "examples": "docusaurus-examples",
    "start": "npm run build-css && docusaurus-start",
    "build": "npm run build-css && docusaurus-build",
    "build-css": "node-sass css/main.scss static/css/custom.css",
    "watch-css": "node-sass --watch css/main.scss static/css/custom.css",
    "publish-gh-pages": "npm run build-css && docusaurus-publish",
    "write-translations": "docusaurus-write-translations",
    "version": "docusaurus-version",
    "rename-version": "docusaurus-rename-version"
  },
  "devDependencies": {
    "docusaurus": "^1.4.0",
    "node-sass": "^4.9.3"
  }
}
